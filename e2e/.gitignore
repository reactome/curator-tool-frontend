# Playwright dependencies and run artifacts. Only the specs, the fixtures, the config, and
# package.json/package-lock.json belong in the repo; everything here is regenerated by
# `npm install && npm run install-browsers` and by running the suite.
node_modules/
test-results/
playwright-report/
blob-report/
.last-run.json
.cache/
