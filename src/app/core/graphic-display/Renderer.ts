import { Renderable } from './Renderable';
import { Graphics } from './Graphics';

export class Renderer {
  constructor() {
  }

  public setRenderable(r: Renderable): void {
  }

  public render(g: Graphics): void {
  }
}
